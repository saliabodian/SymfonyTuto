{% extends 'base.html.twig' %}

{% block title %}Wanna register?{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-sm">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn">Register</button>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
